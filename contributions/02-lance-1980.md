# Contribution 2: Investigate `n_jobs` support for `lance.write_dataset`

**Contribution Number:** 2
**Student:** Adiba Akter
**Project:** Lance (`lance-format/lance`)
**Repository:** https://github.com/lance-format/lance
**Issue:** [#1980 — Add `n_jobs` parameter to `lance.write_dataset`](https://github.com/lance-format/lance/issues/1980)
**Fork:** https://github.com/AdibaAdi/lance
**Contribution repo (public):** https://github.com/AdibaAdi/su26-ai301-contribution
**Branch:** [`fix-1980-n-jobs-write-dataset`](https://github.com/AdibaAdi/lance/tree/fix-1980-n-jobs-write-dataset)
**Status:** Phase I complete · Phase II complete · Phase III complete · Phase IV not started (no upstream PR opened yet)

---

## Contribution Summary

This contribution investigates Lance issue #1980: adding an `n_jobs`-style parallelism
parameter to `lance.write_dataset` so users can write large in-memory tables faster.

The reporter observed that a single `write_dataset` call uses `< 10%` CPU and `~20%`
network while writing a 17 GB in-memory table to Azure Blob Storage, and that running
several writes in parallel threads increased aggregate throughput substantially. A
Lance maintainer confirmed the writer treats input as a single stream (one file at a
time) and explicitly agreed a `n_jobs` parameter would be welcome, posting a reference
`write_lance_parallel` implementation.

For Phase I, I completed repository setup and codebase orientation, confirmed the issue
is still valid and open, and posted an introduction comment. For Phase II, I built a
temporary local benchmark (kept outside the repo) that reproduces the reporter's
original parallel-write observation and also exercises the maintainer's accepted
implementation shape, verifying correctness in every run. No Lance source has been
changed and no PR has been opened.

---

## Why I Chose This Issue

I chose issue #1980 because it sits at the intersection of Python ergonomics and
Rust systems performance, which is exactly the kind of full-stack work I want more
practice with. It is labeled `good first issue` and `enhancement`, yet it is not
trivial: parallel writing touches fragments, commits, and transaction semantics, so
it rewards careful investigation before any code is written. The issue is unusually
well-scoped for a newcomer because a maintainer (`wjones127`) already triaged it,
explained the root cause, and posted a concrete reference implementation, which gives
me a clear and pre-validated target instead of a guess. It is also a real,
user-reported pain point — a 17 GB table taking minutes to write while the machine
sits mostly idle — so the improvement has obvious practical value. Finally, it lets me
practice the discipline of reproducing and measuring a performance claim before
proposing an API change, rather than jumping straight to implementation. That
measure-first workflow is the main skill I wanted to build in this contribution.

---

## Problem Summary

`lance.write_dataset` currently processes its input as a single stream and writes one
file at a time, so a single call cannot saturate available CPU or network when the
source is a large fully in-memory table. The reporter measured meaningful throughput
gains only by manually running multiple writes across threads, which is awkward and
undocumented. The requested change is a first-class `n_jobs`-style parameter that lets
`write_dataset` fan a large in-memory table out into parallel fragment writes and then
commit them as one dataset. The core design question is where that parallelism should
live (Python orchestration vs. the Rust `WriteParams` path) and how to keep fragment,
commit, and error semantics correct.

---

## Repository Activity & Issue-Selection Evidence

Verified on 2026-07-10 against local `upstream` (`lance-format/lance`, HEAD `3e545326`):

- **Actively maintained:** 148 commits in the last 30 days; 78 distinct contributors in
  the last 90 days.
- **Issue state:** `OPEN`, created 2024-02-19, labels `enhancement` and
  `good first issue`.
- **Maintainer engagement / scope confirmation:** `wjones127` (CONTRIBUTOR) explained
  the root cause ("The writer just writes one file at a time, and treats the input data
  as a stream") and explicitly accepted the direction: *"Yeah we could add an `n_jobs`
  parameter to `write_dataset`."* He later posted a full `write_lance_parallel`
  reference script, saying *"I would welcome a PR to add this parameter."*
- **Reporter clarification:** the payload is *"one big table in memory"*, ~17 GB
  (`5m+` rows / `200+` columns) — an in-memory input, which is exactly the case a
  first-class parallel write can safely partition.

### Introduction comment (posted)

I posted an introduction comment on the issue on 2026-06-15 (GitHub user `AdibaAdi`):

> Hello maintainers, I'd like to work on this issue. I'm planning to start by
> reproducing the current `lance.write_dataset` behavior with a large in-memory table,
> then trace where the Python `write_dataset` API delegates into the writer
> implementation. Before implementing, I wanted to confirm whether adding an `n_jobs`
> parameter is still the preferred direction or whether there is a newer
> writer/concurrency API I should align with. I'll keep the initial scope small:
> reproduce the performance gap, identify the relevant code path, propose a minimal
> implementation plan and add/update tests if the approach is still desired.

---

## Environment Setup

**Setup method:** the repository's prescribed Python workflow — `uv` for environment
management, which builds the local `pylance` Rust extension as part of `uv sync`
(`make install`). Python commands are run via `uv run` (per `python/AGENTS.md` and
`python/CONTRIBUTING.md`). Rust toolchain via `rustup`.

```bash
# Fork already added as origin; upstream tracks lance-format/lance
git remote -v
#   origin    https://github.com/AdibaAdi/lance.git
#   upstream  https://github.com/lance-format/lance.git

git checkout -b fix-1980-n-jobs-write-dataset origin/main   # already created

# Python bindings + native extension (from python/)
uv sync            # builds the pylance Rust extension (slow on first run — expected)
```

**Real obstacles encountered and how they were resolved**

1. **`lance` is a compiled extension, not pure Python.** A bare `python -c "import lance"`
   fails with `ModuleNotFoundError: No module named 'lance'` because the module only
   exists inside the `uv`-managed environment that built the native extension. Resolved
   by running everything through the project environment, e.g.:
   ```bash
   uv run --project /Users/adibaakter/dev/lance/python python -c "import lance; print(lance.__version__)"
   # -> lance 9.0.0-beta.10   (pyarrow 24.0.0)
   ```
   This matches the documented "common failure mode": a missing-module error from bare
   `python` is an environment-usage mistake, not a missing dependency.
2. **Source paths had to be confirmed against the tree.** The Python public API lives at
   `python/python/lance/dataset.py` (note the doubled `python/python`), and fragment
   helpers at `python/python/lance/fragment.py`. Verified with `find` before citing.
3. **macOS open-file limit (precaution, not a failure).** `python/CONTRIBUTING.md`
   recommends `ulimit -n 2048` on OS X for the test/benchmark suite. I set it preventively
   before the benchmark; no file-descriptor error actually occurred in these runs.

---

## Reproduction

The benchmark script is kept **outside** the repository at `/tmp/lance_issue_1980_repro.py`.
It is self-contained and uses only existing dependencies (`pyarrow`, `numpy`, `lance`,
and the standard-library `concurrent.futures.ThreadPoolExecutor`) — no new `joblib`
dependency.

### How to run (numbered, executable without prior context)

1. Ensure the Lance Python environment is built once:
   ```bash
   cd /Users/adibaakter/dev/lance/python && uv sync
   ```
2. Raise the macOS open-file limit (precaution) and run the script through the project
   environment:
   ```bash
   ulimit -n 2048
   uv run --project /Users/adibaakter/dev/lance/python python /tmp/lance_issue_1980_repro.py
   ```
3. The script generates in-memory PyArrow tables, runs a warm-up plus 3 measured
   repetitions per case, prints median elapsed time, throughput, fragment counts, and a
   correctness verdict, then deletes all temporary datasets.

The script measures two scenarios:

- **Scenario A (original-reporter reproduction).** Several independent in-memory tables
  are written to **separate** dataset URIs, sequentially vs. concurrently via a
  `ThreadPoolExecutor`. This mirrors what the reporter actually did (many tables, each in
  its own thread).
- **Scenario B (maintainer-aligned implementation target).** **One** in-memory table is
  split into `n_jobs` parts, each part written with `lance.fragment.write_fragments` in a
  thread, then all fragments committed to a **single** dataset via a `LanceOperation`.
  This is the exact shape of `wjones127`'s accepted `write_lance_parallel` reference and
  the most likely internal behavior of an in-call `write_dataset(..., n_jobs=)` parameter.

Each output dataset is verified: row count, schema (column names), full content equality
on the `id` and `val` columns (after sorting by `id`), and a representative checksum on
the `vec` column, plus the resulting fragment count.

### Expected behavior

`lance.write_dataset` should let a caller opt into parallelism for a large in-memory
table (e.g. `n_jobs=4`) and use more of the available CPU/network, reducing wall-clock
write time while producing a dataset that is byte-for-byte equivalent in row count,
schema, and content to a sequential write.

### Actual behavior (current API)

`lance.write_dataset` exposes **no** `n_jobs` / `num_workers` / `num_threads` /
`write_parallelism` parameter. The only way to parallelize today is to orchestrate it
externally (multiple `write_dataset` calls, or manual `write_fragments` + commit), which
is undocumented on this API and easy to get wrong (fragment collection, commit mode,
error handling).

---

## Results

**Hardware / storage disclaimer.** All numbers below are **local macOS temporary-storage
(local SSD) results**, not Azure Blob Storage and not production-scale proof. The
reporter's bottleneck was network I/O to object storage; local SSD has a very different
profile. These runs demonstrate the *mechanism* and *correctness*, and give a
conservative lower bound on the benefit — a network-bound backend would be expected to
show larger gains, but that is not proven here.

**Run environment (this session):**

```text
host      : macOS-26.5.1-arm64
cpu count : 10
lance     : 9.0.0-beta.10    pyarrow: 24.0.0
config    : 8 tables x 250,000 rows x dim 128  ->  2,000,000 rows total (~1.05 GB in memory)
method    : 1 warm-up + 3 measured reps, median reported
```

### Scenario A — original-reporter reproduction (multiple tables → multiple URIs)

| n_jobs | sequential median | concurrent median | speedup | tables/s seq → con | fragments/dataset | correctness |
|-------:|------------------:|------------------:|--------:|-------------------:|:-----------------:|:-----------:|
| 2      | 0.900 s           | 0.525 s           | 1.71×   | 8.89 → 15.23       | 1 each            | PASS |
| 4      | 0.883 s           | 0.386 s           | 2.29×   | 9.06 → 20.74       | 1 each            | PASS |

### Scenario B — maintainer-aligned target (one table → N fragments → one dataset)

Single 250,000-row table split into `n_jobs` parts via `write_fragments` + commit:

| n_jobs | sequential median | concurrent median | speedup | fragments created | rows    | correctness |
|-------:|------------------:|------------------:|--------:|:-----------------:|:-------:|:-----------:|
| 2      | 0.122 s           | 0.077 s           | 1.58×   | 2                 | 250,000 | PASS |
| 4      | 0.119 s           | 0.051 s           | 2.35×   | 4                 | 250,000 | PASS |

### Interpretation

- The current API has no parallel-write parameter, confirming the gap.
- Concurrent writes were faster than sequential writes in every case (1.58×–2.35×), even
  on local SSD where I/O is not the bottleneck, because the Rust write path releases the
  GIL and encoding parallelizes across cores.
- **Correctness held in every run**: row counts, schema, and content matched the source,
  and fragment counts were exactly as expected (Scenario B produced `n_jobs` fragments in
  one dataset, matching the maintainer's design).
- These are small local measurements; absolute speedups will vary with hardware, data
  size, and storage backend. They justify continuing to implementation planning, not a
  production performance claim.

---

## Root-Cause Hypothesis

This is an enhancement, not a defect, so the "root cause" is a design limitation:
`lance.write_dataset` delegates to a Rust write path whose `WriteParams` has **no**
notion of intra-write parallelism, and the writer consumes its input as one sequential
`RecordBatch` stream writing one file at a time (confirmed by the maintainer and by the
absence of any worker-count field in `WriteParams`). Consequently a single large
in-memory input cannot be fanned out across cores or connections within one call, so CPU
and network stay under-utilized. The fix is to add an opt-in worker count that partitions
an in-memory input and writes fragments concurrently, then commits them as one dataset.

---

## Exact Files & Functions Involved

Referenced by symbol first (line numbers are approximate, as of HEAD `3e545326`, since
they drift):

- **Python public API:** `write_dataset(...)` in `python/python/lance/dataset.py`
  (around line 7155). Existing write knobs here: `max_rows_per_file`,
  `max_rows_per_group`, `max_bytes_per_file`, `mode`, `data_storage_version`,
  `storage_options`. No worker-count parameter.
- **Python fragment helper:** `write_fragments(...)` in `python/python/lance/fragment.py`
  — the in-memory-friendly primitive the maintainer's reference builds on.
- **Python → Rust bridge:** `python/src/dataset.rs`.
- **Rust write parameters:** `struct WriteParams` in `rust/lance/src/dataset/write.rs`
  (around line 267) — the struct a Rust-level worker count would extend.
- **Existing tests:** write coverage in `python/python/tests/test_dataset.py`.

### Strongest "Match" in the repository

`CompactionOptions.num_threads: Option<usize>` in `rust/lance/src/dataset/optimize.rs`
(around line 189) is a near-exact precedent for the parameter this issue wants:

- Its doc comment says *"The number of threads to use ... Defaults to the number of
  compute-intensive CPUs."*
- It is consumed as
  `...num_threads.unwrap_or_else(get_num_compute_intensive_cpus)` inside a
  `buffer_unordered(...)` concurrency limit (around line 805) — i.e. `None` means "use
  the sensible default", a concrete value caps parallelism.
- The default helper is `get_num_compute_intensive_cpus()` in
  `rust/lance-core/src/utils/tokio.rs` (around line 19), which caches `num_cpus::get()`,
  honors the `LANCE_CPU_THREADS` env override, and reserves I/O cores.

This gives a project-blessed answer to several design questions: an `Option<usize>` where
`None` preserves current behavior via a CPU-based default, and a reusable helper instead
of hand-rolled concurrency. On the Python side, `batch_readahead` / `fragment_readahead`
(`Optional[int] = None`, wired via `setopt` in `dataset.py`) show the matching Pythonic
convention for optional concurrency knobs.

---

## UMPIRE Implementation Plan

### Understand

- **User problem:** a single `write_dataset` call on a large in-memory table under-uses
  CPU/network; parallelism is only achievable via awkward external orchestration.
- **Reproduction:** `/tmp/lance_issue_1980_repro.py` (above); concurrent writes are
  1.58×–2.35× faster locally with identical correctness.
- **Expected vs. actual:** expected an opt-in `n_jobs`; actual API has none.
- **Current call path:** `lance.write_dataset` (Python) → `python/src/dataset.rs` (PyO3)
  → Rust write path driven by `WriteParams` (single stream, one file at a time).

### Match

- **Comparable implementation:** `CompactionOptions.num_threads: Option<usize>` in
  `optimize.rs`, defaulted via `get_num_compute_intensive_cpus()` and used as a
  `buffer_unordered` limit — the pattern to imitate.
- **Comparable validation/testing pattern:** `python/python/tests/test_dataset.py` write
  tests; project standard is `@pytest.mark.parametrize` for input-only variations and
  `assert`-based correctness (no timing assertions).
- **Relevant history/context:** the accepted maintainer reference (`write_lance_parallel`)
  splits one table into parts, writes fragments per part in threads, and commits with
  `LanceOperation.Append`/`Overwrite`.

### Plan

- **Preferred (minimal, maintainer-aligned):** add `n_jobs: Optional[int] = None` to the
  Python `write_dataset`. When set `> 1` **and** the input is a fully materialized
  in-memory table (`pa.Table` / equivalent), partition into `n_jobs` slices, call
  `write_fragments` per slice concurrently, then commit all fragments as one dataset
  (`Append` if it exists, else `Overwrite`) — i.e. productionize the maintainer's
  reference behind the public API. `None`/`1` → today's exact single-stream path.
- **Alternative (deeper):** thread a worker count into Rust `WriteParams` so the core
  writer fans out internally, reusing `get_num_compute_intensive_cpus()` for the default.
  More robust and shared across bindings, but larger and needs maintainer buy-in.
- **Files likely to change:** `python/python/lance/dataset.py` (param + validation +
  docstring), possibly `python/src/dataset.rs` and `rust/lance/src/dataset/write.rs` for
  the alternative; tests in `python/python/tests/test_dataset.py`.
- **Backward compatibility:** `n_jobs=None` (default) must be byte-for-byte identical to
  current behavior; no signature break.
- **Parameter semantics:** `None` and `1` → sequential (unchanged). `>= 2` → that many
  parallel fragment writers. Reject `0`, negatives, and non-integers with a descriptive
  error at the API boundary (per repo standard: never silently clamp). Avoid `-1` "all
  cores" magic unless maintainers want it, since it is inconsistent with the existing
  `num_threads`/`None`-default convention.
- **Error handling:** if any worker fails, surface the first error and do **not** commit a
  partial dataset (all-or-nothing commit).
- **Tests:** parametrized correctness tests (`n_jobs` ∈ {None, 1, 2, 4}) asserting equal
  row count/schema/content and expected fragment count; multi-fragment scenario;
  validation tests for `0`/negative/non-integer; NULL/empty-table edge cases.
- **Documentation:** docstring with the semantics above and a short example; note it
  applies to in-memory inputs.

### Review

- **Risks:** fragment/commit correctness under concurrency; transaction atomicity
  (partial failure must not commit); ordering across fragments; thread safety of the
  PyO3 boundary; input types that are streaming/lazy and cannot be safely partitioned;
  behavior differences on cloud object stores (the reporter's real target) vs. local SSD.
- **Questions for maintainers:** Python-level orchestration vs. Rust `WriteParams`? Should
  `n_jobs` silently no-op for non-in-memory/streaming inputs or raise? Preferred default
  semantics and whether to reuse `get_num_compute_intensive_cpus()`? Is `Append` vs.
  `Overwrite` selection acceptable, or should mode be explicit?
- **Why appropriately scoped:** the preferred plan reuses an accepted reference and an
  existing primitive (`write_fragments`), changes one public signature additively, and
  defers the deeper Rust change unless requested.

### Evaluate

- **Objective correctness criteria:** for every `n_jobs`, the resulting dataset equals the
  sequential result in row count, schema, and content; fragment count matches
  expectation; invalid `n_jobs` raises.
- **Targeted automated tests:** the parametrized `write_dataset` tests above in
  `python/python/tests/test_dataset.py`.
- **Broader regression:** `uv run pytest python/python/tests/test_dataset.py -k "write"`
  plus the existing suite.
- **Manual benchmark method:** the `/tmp` script (median of 3 reps after warm-up),
  ideally re-run against an object-store backend before claiming production benefit.
- **Explicit note:** timing/speedup assertions must **not** be placed in unit tests —
  they are environment-dependent and flaky; unit tests assert correctness only.

---

## Edge Cases to Cover

- `n_jobs=None` and `n_jobs=1`: identical to current behavior.
- `n_jobs=0`, negative, or non-integer: descriptive error.
- `n_jobs` greater than row count / tiny tables: must not crash or produce empty fragments.
- Empty table and all-NULL columns: correct schema, zero/expected rows.
- Streaming / non-materialized inputs: defined behavior (no-op-to-sequential or explicit
  error) rather than an unsafe partition.
- Worker failure mid-write: no partial commit.

---

## Phase III: Implementation

Phase III is **complete**. The implementation lives on the fork branch and is committed
there; **no upstream Lance pull request has been opened** (that is Phase IV), and **no
maintainer feedback on this implementation has been received yet**.

- **Branch:** [`fix-1980-n-jobs-write-dataset`](https://github.com/AdibaAdi/lance/tree/fix-1980-n-jobs-write-dataset)
- **Commit:** [`d1aa0e127`](https://github.com/AdibaAdi/lance/commit/d1aa0e127) —
  `feat(python): add parallel overwrite support to write_dataset`
- **Files modified:**
  - `python/python/lance/dataset.py`
  - `python/python/tests/test_dataset.py`

### Implementation summary

Added an optional `n_jobs` argument to `lance.write_dataset`:

- `n_jobs=None` (default) and `n_jobs=1` preserve the existing sequential write path
  exactly — no behavior change for existing callers.
- `n_jobs >= 2` enables parallel writing for a materialized `pyarrow.Table` with
  `mode="overwrite"`:
  - the table is divided into contiguous, non-empty partitions;
  - partitions are written concurrently through `lance.fragment.write_fragments`
    (via `concurrent.futures.ThreadPoolExecutor`, a standard-library tool — **no new
    dependency**);
  - the resulting fragments are collected in **deterministic partition order**;
  - all fragments are committed in **one final `LanceOperation.Overwrite` transaction**.
- **Effective workers are capped at the number of non-empty partitions**, so a small
  table with a large `n_jobs` never spawns idle or empty workers.
- **A worker failure prevents the final commit** — the first worker error is re-raised
  before the commit, so no partial dataset version is produced. (Fragment files already
  written by completed workers may remain uncommitted in storage; this matches the
  documented behavior of the low-level `write_fragments` API and is noted in the
  docstring.)
- **Parallel `create` and `append` are explicitly rejected** (clear error), because the
  public fragment-commit path cannot safely reproduce their concurrency/transaction
  guarantees (see *Engineering challenge* below).
- **Unsupported input types and incompatible options raise clear errors** rather than
  silently degrading to a sequential write.

### Implementation decisions

- **Python-level orchestration** (the maintainer-endorsed `write_lance_parallel` shape),
  not a Rust `WriteParams` change — kept the first contribution small, additive, and
  reviewable. The deeper Rust-core option is recorded as future work.
- **`mode="overwrite"` only** for parallel writes. `LanceOperation.Overwrite`
  ("overwrite or create", no `read_version`) is the one commit shape whose semantics the
  Python fan-out can reproduce faithfully; `create` and `append` are rejected rather than
  weakened.
- **Kept the parameter name `n_jobs`** because that is the exact name requested in issue
  #1980. Lance's other worker-count knobs use names like `num_workers` /
  `*_concurrency`; the naming choice is flagged as a maintainer discussion point for the
  eventual PR rather than pre-emptively renamed.
- **Reject-don't-clamp** for invalid values, per the repo's error-handling standard
  (`bool`, non-integer, `0`, and negatives all raise with a descriptive message).

### Engineering challenge and resolution

The first draft attempted to support parallel `create`, `overwrite`, **and** `append`.
A code review found that Python-level fragment commits could **not** safely preserve:

- the **atomic create-only** guarantee the native writer enforces (it raises
  "dataset already exists"); nor
- **append's conflict-detection / retry** semantics (the native append path retries on a
  commit conflict; a blind `Overwrite(read_version=None)` can silently replace a
  concurrently-created dataset).

Rather than ship modes whose concurrency guarantees were quietly weakened, I deliberately
**narrowed the scope to parallel `overwrite`**, which is provably correct with the
existing public API. This also removed the fragile dataset-existence detection and
error-message parsing, shrinking the diff and making the contribution easier to review.
Parallel `create`/`append` are documented as **future work** that likely needs stronger
transaction primitives or a Rust-core implementation (which owns those semantics and
could be shared across all bindings).

### Test commands and exact results

```bash
# Targeted n_jobs tests
uv run pytest python/tests/test_dataset.py -k "n_jobs" -q       # 29 passed

# Full module regression
uv run pytest python/tests/test_dataset.py -q                   # 227 passed, 5 skipped

# Formatting, linting, type/clippy checks
uv run make format      # passed (no changes)
uv run make lint        # passed — Pyright: 0 errors; Clippy: passed
git diff --check        # passed (no whitespace/conflict errors)
```

Repository **pre-commit hooks passed** on the implementation commit: `ruff`,
`ruff-format`, `typos`.

> The 5 skipped tests and 3 warnings in the full run are pre-existing and unrelated to
> this change (e.g. the deprecated `ShardedBatchIterator`).

### Edge cases covered by tests

- `n_jobs=None` and `n_jobs=1` (sequential compatibility across `create`/`overwrite`/`append`)
- parallel worker counts `2` and `4` (correctness: row count, schema, content)
- `n_jobs=1` produces the same fragment layout as the sequential path
- zero and negative worker counts → `ValueError`
- Boolean and non-integer values → `TypeError`
- worker count larger than row count (oversized) → capped, no empty fragments
- empty table (no zero-worker executor; valid empty dataset)
- null-valued columns
- unsupported input types (e.g. `RecordBatchReader`, pandas) → clear error
- unsupported options (e.g. `commit_message`) → clear error
- explicit `schema=` with parallel mode → rejected (not silently ignored)
- unsupported parallel `create` and `append` modes → clear error
- worker failure before final commit → no dataset version created
- multiple fragments produced (asserted `> 1`, **without** guaranteeing an exact count)

### Known limitations (documented as future work)

- Parallel writes are limited to in-memory `pyarrow.Table` + `mode="overwrite"`.
- Parallel `create`/`append` are not supported (transaction-semantics reasons above).
- On worker failure, orphan fragment files may remain uncommitted in storage.
- Speedup evidence is local-SSD only (Phase II); object-store/production gains are
  expected but not proven here.

### Phase III completion status

Implementation, tests, formatting, linting, and local pre-commit hooks all pass on the
fork branch. Phase III is complete. **No upstream Lance PR exists yet** — opening and
iterating on that PR is the Phase IV next step.

---

## Testing Strategy

Tests were added in the Phase III commit (`d1aa0e127`) in
`python/python/tests/test_dataset.py`. Verified results:

```bash
uv run pytest python/tests/test_dataset.py -k "n_jobs" -q   # 29 passed
uv run pytest python/tests/test_dataset.py -q               # 227 passed, 5 skipped
```

- [x] Parametrized sequential-compatibility tests over `n_jobs ∈ {None, 1}` across
      `create`/`overwrite`/`append` (equal row count/schema/content vs. sequential).
- [x] Parametrized parallel-overwrite correctness over `n_jobs ∈ {2, 4}`.
- [x] Validation tests for invalid `n_jobs` (`0`, negative, non-integer, `bool`).
- [x] NULL-column and empty-table edge cases.
- [x] Oversized worker count (capped at non-empty partitions).
- [x] Unsupported input types, unsupported options, and explicit `schema=` all rejected.
- [x] Unsupported parallel `create`/`append` modes rejected.
- [x] Worker failure before commit → no dataset version created.
- [x] Multiple fragments produced, **without** asserting an exact fragment count.
- [x] Correctness assertions only — no timing assertions in unit tests (benchmarking
      stays in the external script).

---

## Risks and Open Questions

**Open questions (need maintainer confirmation):**

- Python-level orchestration vs. Rust `WriteParams` worker count?
- Should `n_jobs=None` be the exact current path, and should the default reuse
  `get_num_compute_intensive_cpus()`?
- Should invalid values (`0`, negative, non-integer) raise (my assumption: yes)?
- Should streaming inputs be rejected or silently fall back to sequential?
- Commit mode selection (`Append` vs `Overwrite`) — implicit or explicit?

**Risk areas:**

- Over-scoping the first PR (mitigated by the minimal Python-first plan).
- Changing write behavior for existing users (mitigated by `None` default).
- Partial commits on worker failure (must be all-or-nothing).
- Presenting local SSD speedups as object-store/production proof (explicitly avoided).

---

## Status

```text
Phase I:   Complete  (setup, orientation, issue posted)
Phase II:  Complete  (reproduction + baseline measured, correctness verified)
Phase III: Complete  (parallel-overwrite implemented + tested on fork branch; commit d1aa0e127)
Phase IV:  Not started (no upstream PR opened; no maintainer feedback yet)
```

Branch: [`fix-1980-n-jobs-write-dataset`](https://github.com/AdibaAdi/lance/tree/fix-1980-n-jobs-write-dataset).
Implementation committed to the fork at
[`d1aa0e127`](https://github.com/AdibaAdi/lance/commit/d1aa0e127). **No upstream Lance
pull request has been opened yet**, and **no maintainer feedback on this implementation
has been received yet**.

### Remaining manual CodePath / GitHub actions (not yet done)

- Submit the CodePath **Phase III** check-in form (cannot be verified or completed from
  the code environment).
- **Phase IV:** open the upstream Lance pull request from the fork branch, referencing
  issue #1980, and raise the two open maintainer questions (Python-vs-Rust placement;
  `n_jobs` vs. `num_workers` naming). Iterate on review feedback.
- (Phase I/II check-ins were submitted previously and Phase I/II content was pushed in
  CodePath commit `d13c9ae`.)

---

## Learnings and Reflections

### Technical skills gained

- Traced a Python public API through a PyO3 bridge into a Rust write path.
- Learned to read `WriteParams` and compaction options to find a project-blessed
  precedent (`num_threads` + `get_num_compute_intensive_cpus`) before designing a new one.
- Practiced reproduce-and-measure discipline with medians, warm-up, and correctness
  checks instead of asserting a speedup blindly.

### Challenges

- `lance` is a compiled extension; commands must go through the `uv` environment.
- The interesting part of a "good first issue" was the design (fragments, commits,
  transactions), not the parameter itself.
- Local SSD is a poor proxy for the reporter's network-bound Azure scenario, so results
  must be framed carefully.

### What I would do differently

- Confirm the accepted design in the issue thread first (I did, and it reframed which
  scenario is the true implementation target).
- Search for an existing equivalent parameter early — the `num_threads` precedent saved
  significant design guesswork.

---

## Resources Used

- Lance issue #1980: https://github.com/lance-format/lance/issues/1980
- Lance repository: https://github.com/lance-format/lance
- My Lance fork: https://github.com/AdibaAdi/lance
- Contribution repo: https://github.com/AdibaAdi/su26-ai301-contribution
- Lance Python docs: https://lancedb.github.io/lance/
- `write_fragments` docs: https://lancedb.github.io/lance/api/python/lance.html#lance.fragment.write_fragments
- PyArrow Table: https://arrow.apache.org/docs/python/generated/pyarrow.Table.html
- `concurrent.futures`: https://docs.python.org/3/library/concurrent.futures.html
- Rust install: https://www.rust-lang.org/tools/install
- `uv`: https://docs.astral.sh/uv/

---

## Next Steps

Phase III (implementation) is complete on the fork branch. Remaining work is Phase IV:

1. Submit the CodePath Phase III check-in.
2. Open the upstream Lance pull request from `fix-1980-n-jobs-write-dataset` (commit
   `d1aa0e127`) referencing issue #1980, describing the scope (parallel `overwrite` for
   in-memory `pyarrow.Table`) and the deferral of parallel `create`/`append`.
3. Raise the open maintainer questions in the PR: Python-level orchestration vs. a
   Rust-core `WriteParams` implementation, and the `n_jobs` vs. `num_workers` naming.
4. Iterate on maintainer review feedback (none received yet).
